# Scripts

This directory will contain public command-line entry points for reproducing the
JADT 2026 paper results.

The package is being developed so that reproduction does not depend on any
private research workflow software. Scripts should be runnable with a standard
Python environment after installing the package with `pip install -e .`.
